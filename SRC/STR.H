#ifndef _STR_H_
#define _STR_H_

//archivo str.h
//algunas funciones utiles para manejar strings
#include <string>

string Ingresar(char*);
char Mayuscula(char);
char Minuscula(char);
string Minuscula(string);
bool EsEntero(string sString, int ni=0 );
int Entero(string sString, int ni=0);
#include "str.h"

#endif
